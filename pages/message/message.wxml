<view class="message">
  <card
    avatar="{{user.avatar}}"
    userName="{{user.userName}}"
    lastChat="{{user.lastChat}}"
    unReadNum="{{user.unReadNum}}"
    lastTime="{{user.lastTime}}"
    bindtap="enterChat"
  ></card>
</view>
